<UserControl x:Class="VS2013Test.Views.LogPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VS2013Test.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding LoadingText}" />
        <!--<ScrollViewer x:Name="scroller" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical">                
                <ListBox ItemsSource="{Binding AllLogRow}" >
                    <ListBox.Template>
                        <ControlTemplate>
                             --><!--Empty template to allow ScrollViewer to capture mouse scroll--><!-- 
                            <ItemsPresenter />
                        </ControlTemplate>
                    </ListBox.Template>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Text}" FontFamily="Arial" FontSize="20" Margin="-3" Foreground="{Binding ForegroundColor}" Background="{Binding BackGroundColor}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </ScrollViewer>-->

        <DataGrid Grid.Row="1" x:Name="dataGrid" ItemsSource="{Binding AllLogRow}" VirtualizingPanel.IsVirtualizing="True" AutoGenerateColumns="False" HeadersVisibility="None" IsReadOnly="True" Visibility="{Binding ListVisibility}" IsEnabled="{Binding ListIsEnabled}" >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Text}" />
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{Binding BackGroundColor}"/>
                    <Setter Property="Foreground" Value="{Binding ForegroundColor}"/>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</UserControl>
